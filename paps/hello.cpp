// include input and output stream from  std library
// is a file that contains routines for rea ding and writing data to stream
#include <iostream>

// instead of prefixing with `std::` every time we use the standard library
using namespace std;

int main(){
    cout << "Hello World!" << endl << "continue";
    cout << endl << "hi";
}